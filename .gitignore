#ignoring .pyc to prevent bugs and clutter
*.pyc

#ignore config/keys. If you are forking this repo, please provide your own keys
config/keys

#ignore cache data, but not cache scripts
cache/*
!cache/src